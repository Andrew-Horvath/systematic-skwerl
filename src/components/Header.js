import React from 'react';
import Logo from './Logo';

const Header = props => {
    return (
        <header className="header-custom">
            <Logo />
        </header>
    )
}

export default Header